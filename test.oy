quoted <- '((... 'args) args)

list <- '((... args) args)

unary-\ <- quoted:
    ('args)
    oif (atom-p args):
        set! args: list args
    quoted:
        (... 'code)
        list args @code

#--------------------------------------------------------------#
print: quoted Cons car cdr and fn tests.

print: car: 't :: ()

print: car: 
    (\b: 't :: b :: ()) 'a

print: car: cdr:
    ((\b: \a: a :: b :: ()) 't) 'b


#--------------------------------------------------------------#
print: quoted Scope and leaking tests.

print: a <- 't

print: a
print: (\('b) b) t
print: (\('b) b): really a

b <- 'b
print: (\b b) 't

print: *((\a: leak a 'a) 'b :: ())

#--------------------------------------------------------------#
print: quoted oif tests.

print: oif 'a 't 'a
print: oif () 'a 't

#--------------------------------------------------------------#
print: quoted Elipsis argument tests.

print: car: (\(... 'b) b) t e a q
print: car: car: (\(... 'b) b): really: 't :: 'a :: ()

#--------------------------------------------------------------#
print: quoted Signal handling tests.

print: with-signal-handler:
    \x: car x
    signal 't


